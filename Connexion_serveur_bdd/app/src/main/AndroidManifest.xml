<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.iut.connexion_serveur_bdd" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".EcranPrincipal"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EcranChoixMot" android:label="Saisir mots" />
        <activity android:name=".EcranRechercheListe" android:label="Resultats de la recherche de liste" />
        <activity android:name=".EcranRecuperationListe" android:label="Recuperation de la liste" />

    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
